# Neo Orchestration Report — 2026-03-24T14-05-28Z

**Agent:** Neo (Test Lead)  
**Task:** Test Gap Analysis for `generate.py` Memory Management  
**Status:** COMPLETED  
**Date:** 2026-03-24T14:05:28 UTC

---

## Coverage Baseline

**Current test coverage:** ZERO  
No project-owned test files exist. `tests/` directory missing entirely.

---

## Memory Behaviors With NO Test Coverage

12 untested memory behaviors identified:

1. `variant="fp16"` only for CUDA/MPS (not CPU)
2. `enable_model_cpu_offload()` routing by device
3. `pipe.to(device)` device-specific behavior
4. `del base` execution in refine path
5. `torch.mps.empty_cache()` called on MPS
6. `torch.cuda.empty_cache()` called on CUDA
7. `gc.collect()` called after each branch
8. `del latents, refiner` cleanup sequencing
9. `del text_encoder_2, vae` cleanup
10. Generator device binding (CPU for MPS/CPU offload, device for CUDA)
11. `torch.compile` UNet compilation on CUDA
12. Shared component transfer (`text_encoder_2`, `vae`) to refiner

---

## Test Suite Plan

### Regression Tests (Protecting PR #1 & PR #2)

**22 mock-based unit tests** across 5 files:

- `test_load_base.py` — variant selection, offload routing, device handling (6 tests)
- `test_load_refiner.py` — shared components, offload, device (4 tests)
- `test_generate.py` — cache clearing, gc calls, generator device (9 tests)
- `test_edge_cases.py` — exception safety, repeated calls, shared object lifetime (3 tests)
- `test_device.py` — device detection and dtype selection (baseline tests)

### Critical Gap: Exception Safety Testing

The most important test (`test_generate_refine_path_cleans_up_on_refiner_exception`) will **FAIL** until `generate()` gets a `try/finally` block. This test serves as a gating check.

---

## Output Artifact

Full audit report written to `.squad/decisions/inbox/neo-memory-audit.md`

Includes:
- Complete test code (22 function definitions)
- Rationale for each test
- Hardware-testable vs. mock-based classification
- Test file structure and fixtures

---

## Risk Assessment

**Highest risk (no protection):**
1. Exception mid-flight in refine path — no try/finally, resources leak
2. Shared object lifetime after `del` — subtle GC behavior untested
3. Zero regression tests — PR #1 & #2 fixes could be accidentally reverted

**Verdict:** Code is correct today, but one careless refactor away from re-introducing bugs with no safety net.

---

## Recommended Implementation Order

1. Create `tests/conftest.py` with shared fixtures
2. Implement 22 unit tests (most will fail initially)
3. Review failures — they indicate code that needs fixing
4. Fix code in order: Morpheus issues → Trinity issue #6 (versions) → exception safety (try/finally)
5. Watch all tests turn green
6. Team review and sign-off
